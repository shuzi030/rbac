# 客户信息管理模块使用说明

## 已完成的功能

### 1. 后端代码结构

#### Mapper 层（数据访问层）
- ✅ `CustomerMapper.java` - 客户数据访问接口
- ✅ `CustomerTraceHistoryMapper.java` - 客户跟进记录数据访问接口
- ✅ `CustomerTransferMapper.java` - 客户转移记录数据访问接口
- ✅ `SystemDictionaryItemMapper.java` - 数据字典项数据访问接口

#### Service 层（业务逻辑层）
- ✅ `CustomerService.java` + `CustomerServiceImpl.java`
- ✅ `CustomerTraceHistoryService.java` + `CustomerTraceHistoryServiceImpl.java`
- ✅ `CustomerTransferService.java` + `CustomerTransferServiceImpl.java`
- ✅ `SystemDictionaryItemService.java` + `SystemDictionaryItemServiceImpl.java`

#### Controller 层（控制器层）
- ✅ `CustomerController.java` - 客户管理控制器
- ✅ `CustomerTraceHistoryController.java` - 跟进记录控制器
- ✅ `CustomerTransferController.java` - 转移记录控制器

#### MyBatis XML 映射文件
- ✅ `CustomerMapper.xml` - 客户SQL映射
- ✅ `CustomerTraceHistoryMapper.xml` - 跟进记录SQL映射
- ✅ `CustomerTransferMapper.xml` - 转移记录SQL映射
- ✅ `SystemDictionaryItemMapper.xml` - 数据字典SQL映射

### 2. 功能模块说明

#### 客户管理（CustomerController）

| 功能 | 路由 | 说明 |
|------|------|------|
| 客户列表 | `/customer/list` | 显示所有客户列表，支持关键字搜索和分页 |
| 客户池 | `/customer/customerPool` | 显示未分配销售人员的客户 |
| 潜在客户 | `/customer/potentialCustomer` | 显示状态为潜在客户的列表 |
| 正式客户 | `/customer/formalCustomer` | 显示状态为正式客户的列表 |
| 流失客户 | `/customer/loseCustomer` | 显示状态为流失客户的列表 |
| 失败客户 | `/customer/failCustomer` | 显示状态为失败客户的列表 |
| 添加/编辑客户 | `/customer/input` | 客户表单页面 |
| 保存客户 | `/customer/saveOrUpdate` | 保存或更新客户信息 |
| 删除客户 | `/customer/delete` | Ajax删除客户 |
| 分配销售 | `/customer/assign` | 分配客户给销售人员 |
| 更新状态 | `/customer/updateStatus` | 更新客户状态 |
| 数据统计 | `/customer/echarts` | 客户数据可视化 |

#### 跟进记录管理（CustomerTraceHistoryController）

| 功能 | 路由 | 说明 |
|------|------|------|
| 跟进历史 | `/traceHistory/toTraceHistoryPage` | 显示所有跟进记录 |
| 客户跟进记录 | `/traceHistory/listByCustomerId` | 查询指定客户的跟进记录 |
| 添加/编辑跟进 | `/traceHistory/input` | 跟进记录表单页面 |
| 保存跟进 | `/traceHistory/saveOrUpdate` | 保存或更新跟进记录 |
| 删除跟进 | `/traceHistory/delete` | Ajax删除跟进记录 |
| 最近跟进 | `/traceHistory/listRecent` | 查询客户最近的跟进记录 |

#### 转移记录管理（CustomerTransferController）

| 功能 | 路由 | 说明 |
|------|------|------|
| 转移历史 | `/transfer/toTransferPage` | 显示所有转移记录 |
| 客户转移记录 | `/transfer/listByCustomerId` | 查询指定客户的转移记录 |
| 转移表单 | `/transfer/input` | 客户转移表单页面 |
| 执行转移 | `/transfer/doTransfer` | 转移客户给新销售人员 |
| 批量转移 | `/transfer/batchTransfer` | 批量转移多个客户 |
| 删除记录 | `/transfer/delete` | Ajax删除转移记录 |

### 3. 数据库表说明

#### customer 表（客户表）
```sql
- id: 主键
- name: 姓名
- age: 年龄
- gender: 性别 (0-女, 1-男)
- tel: 电话
- qq: QQ号
- job_id: 职业ID (关联 systemdictionaryitem)
- source_id: 来源ID (关联 systemdictionaryitem)
- seller_id: 销售人员ID (关联 employee)
- inputUser_id: 录入人员ID (关联 employee)
- input_time: 录入时间
- status: 状态 (0-潜在, 1-正式, 2-流失, 3-失败)
```

#### customertracehistory 表（跟进记录表）
```sql
- id: 主键
- trace_time: 跟进时间
- trace_details: 跟进详情
- trace_type_id: 跟进类型ID (关联 systemdictionaryitem)
- trace_result: 跟进结果
- remark: 备注
- customer_id: 客户ID
- input_user_id: 录入人员ID
- input_time: 录入时间
- type: 类型
```

#### customertransfer 表（转移记录表）
```sql
- id: 主键
- customer_id: 客户ID
- operator_id: 操作人员ID
- operate_time: 操作时间
- oldSeller_id: 原销售人员ID
- newSeller_id: 新销售人员ID
- reason: 转移原因
```

### 4. 使用流程

#### 4.1 添加客户
1. 访问 `/customer/input`
2. 填写客户基本信息（姓名、年龄、性别、电话、QQ）
3. 选择职业类型和客户来源
4. 可选择分配销售人员
5. 点击保存，系统自动记录录入人员和时间
6. 客户默认状态为"潜在客户"

#### 4.2 分配客户
1. 访问 `/customer/customerPool` 查看客户池
2. 选择未分配的客户
3. 调用 `/customer/assign` 接口分配给销售人员

#### 4.3 跟进客户
1. 在客户列表中选择客户
2. 访问 `/traceHistory/input?customerId=xxx`
3. 填写跟进信息（跟进时间、跟进详情、跟进类型、跟进结果）
4. 保存跟进记录

#### 4.4 转移客户
1. 访问 `/transfer/input?customerId=xxx`
2. 选择新的销售人员
3. 填写转移原因
4. 调用 `/transfer/doTransfer` 执行转移
5. 系统自动记录转移历史

#### 4.5 更新客户状态
1. 根据跟进情况更新客户状态
2. 调用 `/customer/updateStatus` 接口
3. 状态流转：潜在客户 → 正式客户 / 流失客户 / 失败客户

### 5. 前端页面

确保以下HTML页面存在（项目中已有）：
- ✅ `templates/customer/list.html` - 客户列表
- ✅ `templates/customer/customerpoollist.html` - 客户池
- ✅ `templates/customer/potentialcustomerlist.html` - 潜在客户
- ✅ `templates/customer/formallist.html` - 正式客户
- ✅ `templates/customer/losecustomerlist.html` - 流失客户
- ✅ `templates/customer/failcustomerlist.html` - 失败客户
- ✅ `templates/customer/echarts.html` - 数据可视化
- ✅ `templates/history/tracehistorypage.html` - 跟进历史
- ✅ `templates/history/transferpage.html` - 转移历史

### 6. 权限配置

在 `permission` 表中已有相关权限：
- `kunkun:customer` - 客户列表
- `kunkun:potentialCustomer` - 潜在客户
- `kunkun:customerPool` - 客户池
- `kunkun:failCustomer` - 失败客户
- `kunkun:formalCustomer` - 正式客户
- `kunkun:loseCustomer` - 流失客户
- `kunkun:toTraceHistoryPage` - 跟进历史管理
- `kunkun:toTransferPage` - 移交历史管理

### 7. 测试建议

#### 7.1 启动项目
```bash
mvn spring-boot:run
```

#### 7.2 访问测试
1. 访问 `http://localhost:9090/login.html` 登录系统
2. 导航到客户管理模块
3. 测试以下功能：
   - 查看客户列表
   - 添加新客户
   - 编辑客户信息
   - 查看客户池
   - 分配客户给销售
   - 添加跟进记录
   - 执行客户转移
   - 查看数据统计

#### 7.3 数据库检查
确保数据库中已有：
- `customer` 表中有测试数据
- `systemdictionary` 和 `systemdictionaryitem` 表中有数据字典数据
- `employee` 表中有员工数据

### 8. 注意事项

1. **权限控制**: 所有操作都需要用户登录，Controller中使用 `SecurityUtils.getSubject()` 获取当前用户
2. **事务管理**: Service实现类都添加了 `@Transactional` 注解
3. **关联查询**: Service层自动填充关联对象（职业、来源、销售人员等）
4. **分页查询**: 使用 PageHelper 插件，在 Controller 中通过 `CustomerQuery` 传递分页参数
5. **日期格式**: 使用 `@JsonFormat` 和 `@DateTimeFormat` 注解处理日期格式
6. **状态管理**: 客户状态 0-潜在, 1-正式, 2-流失, 3-失败

### 9. API 接口示例

#### 查询客户列表
```
GET /customer/list?currentPage=1&pageSize=10&keyword=张三&status=0
```

#### 添加客户
```
POST /customer/saveOrUpdate
参数: name, age, gender, tel, qq, jobId, sourceId, sellerId
```

#### 分配客户
```
POST /customer/assign
参数: customerId, sellerId
返回: {"success": true, "msg": "分配成功"}
```

#### 添加跟进记录
```
POST /traceHistory/saveOrUpdate
参数: customerId, traceTime, traceDetails, traceTypeId, traceResult, remark, type
```

#### 执行客户转移
```
POST /transfer/doTransfer
参数: customerId, newSellerId, reason
返回: {"success": true, "msg": "转移成功"}
```

### 10. 扩展建议

可以考虑添加以下功能：
- 客户导入导出功能
- 批量分配客户
- 客户标签管理
- 跟进提醒功能
- 客户转化漏斗分析
- 销售人员业绩排行
- 客户来源分析报表

---

## 总结

客户信息管理模块已完整实现，包括：
- ✅ 完整的三层架构代码（Mapper、Service、Controller）
- ✅ MyBatis XML 映射文件
- ✅ 客户、跟进记录、转移记录的 CRUD 操作
- ✅ 客户状态管理
- ✅ 客户池管理
- ✅ 数据字典集成
- ✅ 权限控制集成
- ✅ 分页查询支持

所有代码遵循项目现有的编码规范，与 Employee 模块保持一致的风格。
